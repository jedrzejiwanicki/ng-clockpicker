import { ClockPickerDirective } from './clock-picker.directive';

describe('ClockPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClockPickerDirective();
    expect(directive).toBeTruthy();
  });
});
