import { MovementEmitterDirective } from './movement-emitter.directive';

describe('MovementEmitterDirective', () => {
  it('should create an instance', () => {
    const directive = new MovementEmitterDirective();
    expect(directive).toBeTruthy();
  });
});
